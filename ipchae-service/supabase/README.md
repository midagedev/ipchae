# Supabase Setup

1. Open `/Users/hckim/repo/ipchae/docs/01_SUPABASE_SCHEMA_AND_RLS.sql`.
2. Apply SQL in Supabase SQL editor.
3. Confirm RLS is enabled and policies are created.
4. Create storage buckets listed in deployment runbook.
5. Add `.env` from `.env.example`.

This workspace currently uses local-first persistence and can sync metadata when authenticated.

